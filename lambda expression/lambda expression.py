# lambda expression

# lambda expression is a small, anonymous function that can be defined inline within a larger expression. It's a shorthand way to create a function without having to declare a separate named function.
# syntax = lambda arguments: expression 

add = lambda a,b : a+b
print(add(5,6))